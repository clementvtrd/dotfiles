claude() {
  local name=${PWD:t}
  local kit_path="$HOME/.sbx_kits/spec-kit.zip"
  if sbx ls --quiet | grep -qx "$name"; then
    sbx run --kit $kit_path --name "$name"
  else
    sbx kit pack --output $kit_path "$HOME/.sbx_kits/spec-kit"
    sbx run claude --kit $kit_path --name "$name"
  fi
}
