# Clementvtrd's Dotfiles

## Installation

### Global installation

Simply run the following command:

```sh
sh install.sh
```

### Module installation

Simply run the following command by changing `<module>` with one of the root directories' name:

```sh
sh <module>/install.sh
```